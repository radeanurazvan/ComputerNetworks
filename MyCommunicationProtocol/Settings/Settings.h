#pragma once

class Settings {
    public:
        static constexpr char* credentialsStore = "credentials.txt";
        static constexpr char* sessionFile = "session.txt";
        static constexpr char* sessionPath = "sessions/session.txt";
};